[package]
name = "angry-hub"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui = "*"
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
gpui-selectable-text = "0.1.0"
